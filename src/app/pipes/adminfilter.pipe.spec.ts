import { AdminfilterPipe } from './adminfilter.pipe';

describe('AdminfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
