import { User } from './user';

export class Car {
    
    user: User = new User();

    carId: number;
    color: string;
    seats: number;
    make: string;
    model: string;
    year: number;
    
}